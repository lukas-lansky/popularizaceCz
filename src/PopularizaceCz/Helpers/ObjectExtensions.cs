using EmitMapper;
using System;
using System.Collections.Generic;
using System.Linq;

namespace PopularizaceCz.Helpers
{
    public static class ObjectExtensions
    {
        /*
        public static V MapTo<U, V>(this U source, V target = null)
            where U : class
            where V : class
        {
            if (target == null)
            {
                return ObjectMapperManager.DefaultInstance.GetMapper<U, V>().Map(source);
            }
            else
            {
                return ObjectMapperManager.DefaultInstance.GetMapper<U, V>().Map(source, target);
            }
        }
        */
    }
}